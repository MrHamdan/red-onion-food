import React from 'react';
import { Button } from 'react-bootstrap';

const Checkout = () => {
    return (
        <div>
            <Button className="mt-5" variant="danger">Checkout Your Food</Button>
        </div>
    );
};

export default Checkout;